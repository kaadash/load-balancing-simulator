export default class {
    constructor({
        numberOfProcessors,
        diffusionParam,
        topology
    }) {
        this.numberOfProcessors = numberOfProcessors;
        this.diffusionParam = diffusionParam;
    }
}